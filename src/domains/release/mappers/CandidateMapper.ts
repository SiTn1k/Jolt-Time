/**
 * CandidateMapper
 *
 * Maps between ReleaseCandidate entity and DTOs.
 * Alias for ReleaseMapper - kept for backward compatibility.
 */

import { ReleaseMapper } from './ReleaseMapper';

export { ReleaseMapper as CandidateMapper };
